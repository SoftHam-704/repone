# Deploy — 2026-06-08 (lote da madrugada)

Tudo commitado e **buildado fresco** (back: `npm run build`; front: `vite build`; ADM: `npm run build`). Mapa: arquivo a subir → o que carrega.

## 1. RepOne — Backend (`backend/dist/...` → mesmo caminho no servidor)

Mais simples: subir a pasta **`dist/src/modules/iris/` inteira** (cobre corpus + mapa + tools + radar) e os 3 controllers/rota abaixo.

| Arquivo `.js` | O que entrega |
|---|---|
| `dist/src/modules/orders/orders.controller.js` | CNPJ no card · suporte ao combobox de cliente · consolidação min=0 · descartar-fila |
| `dist/src/modules/orders/orders.routes.js` | rota `POST /descartar-fila` |
| `dist/src/modules/agenda/agenda.controller.js` | aniversariantes incluem contatos das indústrias |
| `dist/src/modules/financeiro/financeiro.controller.js` | **fix da baixa** (`$2 inconsistent types`) — URGENTE |
| `dist/src/modules/iris/tools/*.js` | 4 tools novas (curva_abc, ranking, comparar_anos, último_preço) + `registrar_lacuna` + `index.js` |
| `dist/src/modules/iris/knowledge/*` | corpus `oficio-*.md` + `mapa-sistema-completo.md` + `index.js` |

→ depois: **`pm2 restart <app>`**

## 2. RepOne — Frontend (sobe a pasta `dist/` inteira)
Cobre: CNPJ no grid · combobox de cliente · aniversariantes (PortalHome) · botão Descartar carrinho · status Bonificação Pendente.
→ **Ctrl+Shift+R** depois (PWA/service worker).

## 3. Manual (servidor de arquivos)
`docs/manual-repone.md` → **`softham.com.br/repone/manual-repone.md`**
(atualiza o leitor online E a IRIS how-to; só subir o `.md`).

## 4. Banco MASTER
Nada a rodar — a tabela `public.iris_lacunas` **já foi criada** (e tem 3 exemplos pra ver a tela do ADM; podem ser excluídos pela própria tela).

## 5. SoftHam-ADM (Recados da IRIS)
- Front: subir **`dist-adm/`** (já buildado).
- Backend: subir `backend-adm/routes/iris-lacunas.cjs` + `backend-adm/server.cjs` e **reiniciar o server do ADM** (pega a rota nova).
- *(SoftHam-ADM não é repo git — versionar/subir pelo método atual do ADM.)*

## ⚠️ NÃO entra neste deploy (B2 — separado)
**Migration global do `normalizar_codigo`** (`backend/migrations/pgadmin_global_fix_normalizar_codigo_zumbi.sql`) — roda no `basesales`, mexe no `public` + 27 tenants. Janela de madrugada, à parte.

## Regra de ouro
`orders.controller.js` foi tocado por VÁRIOS commits hoje — suba a versão **atual** (tem tudo acumulado), nunca uma intermediária.
